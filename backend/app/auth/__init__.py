# =============================================================================
# auth/__init__.py — Authentication & Authorization Package
# =============================================================================
# PURPOSE: Marks the auth/ directory as a Python package.
#
# This package contains:
#   - roles.py         → UserRole enum and permission definitions
#   - passwords.py     → bcrypt password hashing and verification
#   - jwt.py           → JWT token creation and decoding
#   - dependencies.py  → FastAPI dependencies for auth (get_current_user, require_roles)
# =============================================================================
